import {IMessage} from '../IMessage';

export interface IMqttMessage extends IMessage {
  // //channel: string;
  // timestamp: number;
  // receiver?: string,
  // sender?: string,
  // /**
  //  * ms
  //  */
  // timestamp_sub: number;
}
